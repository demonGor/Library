@model LibraryDataEF.BOOKS_REVIEWS

@{
    IEnumerable<LibraryDataEF.BOOKS> CmpList = ViewBag.CmpList;
    ViewBag.Title = "Видалення відгука";
}

<h2>@ViewBag.Title</h2>

<h3>Ви впевнені, що хочете видалити цей відгук?</h3>
<div>
    
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.BR_R)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.BR_R)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.BOOKS.BK_NAME)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.BOOKS.BK_NAME)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Видалити" class="btn btn-default" /> |
            @Html.ActionLink("Назад", "BookReviews", new { id = CmpList.First().BK_ID })
        </div>
    }
</div>
